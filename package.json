{
  "name": "meus-jogos",
  "version": "1.0.0",
  "description": "App React com JSON Server",
  "main": "index.js",
  "homepage": "https://laysegabi.github.io/meus-jogos",
  "scripts": {
    "start": "concurrently \"npm run start-react\" \"npm run start-api\"",
    "build": "react-scripts build",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start-react": "react-scripts start",
    "start-api": "json-server --watch db.json --port 4000"
  },
  "dependencies": {
    "bootstrap": "^5.3.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-scripts": "5.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.0.0",
    "json-server": "^0.17.0",
    "gh-pages": "^6.3.0"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": "18.x"
  }
}
